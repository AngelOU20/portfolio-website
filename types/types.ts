import { links as linksEn } from "@/lib/data_en";
import { links as linksEs } from "@/lib/data";

export type SectionNameEn = (typeof linksEn)[number]["name"];
export type SectionNameEs = (typeof linksEs)[number]["name"];
